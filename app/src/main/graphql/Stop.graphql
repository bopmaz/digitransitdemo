query Stop($gtfs_id: String!) {
    stop(id: $gtfs_id) {
        gtfsId
        name
        lat
        lon
        patterns {
            code
            directionId
            headsign
            route {
                gtfsId
                shortName
                longName
                mode
            }
        }
    }
}
