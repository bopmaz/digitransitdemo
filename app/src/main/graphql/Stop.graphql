query Stop($gtfs_id: String!) {
    stop(id: $gtfs_id) {
        name
        stoptimesWithoutPatterns {
            realtimeArrival
            realtimeDeparture
            headsign
        }
    }
}
